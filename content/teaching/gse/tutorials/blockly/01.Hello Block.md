---
title: 01. Hello Block
weight: 1
---

This project overview refers to folder `01.helloBlock` in the [code examples repository](https://github.com/se-buw/blockly-intro).

We create a simple configurable Hello World block that has two input fields, which can be entered by the user. The first input determines how many times the greeting is printed. The second input determines who to greet.

The executable example is available as <a href="../dist/01.helloBlock" target="_blank">hello block language</a>.

## Defining the Block

We start by defining the new block. Blockly provides [different ways to do so](https://developers.google.com/blockly/guides/create-custom-blocks/define/block-definitions). We will use the JSON format as a simple and declarative way to define and adapt new blocks. As a straight forward (but by no means standardized convention) we define blocks in a file `src/blocks.ts` which is a TypeScript file.

{{< sub-code 
  file="code-examples/blockly-intro/01.helloBlock/src/blocks.ts" 
  linenos="true"
>}}

The actual block definition starts in line 6 with the opening curly brace. Common to all blocks 

https://developers.google.com/blockly/guides/create-custom-blocks/fields/built-in-fields/text-input

https://developers.google.com/blockly/guides/create-custom-blocks/fields/built-in-fields/number


Finally, lines 23-24 define connectors for additional blocks above and below this block. The [connectors may be typed](https://developers.google.com/blockly/guides/create-custom-blocks/define/top-level-connections), which is not the case here indicated by value `null`, i.e., there are no restrictions on the types of blocks that can be connected.

## Setting up the workspace and visual editor

Setting up the workspace happens in ``index.html``.

{{< sub-code 
  file="code-examples/blockly-intro/01.helloBlock/src/main.ts" 
  lang="typescript" 
  start="9" 
  end="16" 
  linenos="true"
  hl_lines="2 4-5"
>}}